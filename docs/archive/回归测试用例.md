# NEC2MAA_converter_v03 回归测试用例

> 目的：保证 v03 重构**零行为变化**。全部用例源自 v02 实施期的 12 个测试脚本（`C:\Users\DELL\AppData\Local\Temp\kilo\`），此处固化为文档，任何 v03 批次（尤其 M6 双跑验收）按清单执行。
> 来源明细：v02 的 `NEC2MAA_converter_v02_测试用例.md`（用例定义+预期值）；本文档增加 v03 适配与双跑验收。

## 0. v03 测试基建变化

**脚本加载**：v03 是多文件，Node 桩需按顺序拼接 js/ 六文件后注入（替代"提取单文件 `<script>`"）：

```js
const load = n => fs.readFileSync(`F:\\...\\NEC2MAA_converter_v03\\js\\${n}.js`, 'utf8');
const src = ['state','utils','geometry','extract','convert','app'].map(load).join('\n');
// 其余 DOM 桩结构与 v02 相同（value/style/addEventListener/focus/classList.toggle 必备）
const api = new Function('document','window','alert', src + '\nreturn {...};')(documentStub, windowStub, alertStub);
```

**注意**：app.js 会在 DOMContentLoaded 绑定事件——Node 桩中 `document.addEventListener` 是空函数，绑定不执行（与 v02 行为一致，无影响）。返回对象改为 `N2M` 命名空间内的函数（如 `N2M.extractFromNec`）；`unsupportedErrors` 访问改为 `N2M.state.unsupportedErrors`。

**双跑 diff 验收器（M6 核心工具）**：
```js
// 对同一输入分别跑 v02(单文件提取) 与 v03(六文件拼接)，断言三元组逐字符相等
function runOnce(buildApi, nec, opts) {
  /* 重置 DOM 桩与 N2M.state → extractFromNec(nec, true) → executeConvert() */
  return { out: $('outputMaa').value, alerts: alerts.slice(), warns: state.unsupportedErrors.slice() };
}
// assert.strictEqual(JSON.stringify(runV02(...)), JSON.stringify(runV03(...)))
```

## 1. 必测输入集（12 套）

| # | 输入 | 覆盖 | 对应 v02 脚本 |
|---|---|---|---|
| 1 | 21 段偶极子 + `EX 0 1 11 0 1 0` + FR/GN/GS 矩阵（GN −1/0/1/2 四卡型） | G/H 行、GN 降级告警 | batch3/batch3b |
| 2 | 偏移设计符矩阵：N=1..30 全段枚举 + 百分比/越界 + W8BYA 31 段 s=16 | segToDesignator/getMmanaPos 终版（\|off\|≤1 保留/>1 收敛+告警） | batch2/desig_fix/desig_fix2 |
| 3 | GS 单位后缀 9 形态：ft/135cm/135 cm/2.5in/300mm/SY+ft/xyz 非法/纯数字/无 GS | parseGsScale | batch4 |
| 4 | GM 全用例：绕 Z90°/平移/NRPT=1 阵列(tag→w2c)/NRPT=0+ITG/GX 镜像/多轴告警/GS×GM/连续 GM 指数复制(1→2→4→8) | collectWires | batch5/batch8 |
| 5 | LD 手册七类型矩阵：LD 1/2/3/4/5/0/6/7 + 无 FR + seg_from=0 + 混合 3 行 | parseLoads（含 Q=1.7593、并联等效 48.8178−j7.5969、导纳→50+j0） | batch6 |
| 6 | EX 全类型：W8BYA 原文件（EX 6 双源）+ EX 0/1/4/5 回归 + EX 6 复数 | parseExcitation、exNotes 块外告警 | bugfix_ex_test |
| 7 | 校验告警：Load 坏行×3 形态/GA 多卡去重/SM/扫频 FR I2=5/单频 | validateLoadRows + geomCardWarnings + frSweepNote | batch7 |
| 8 | 参考对照：4x_DJ9BV 反推样例（连续 GM×3 + 4 EX + FR 432.1）11 断言 | 全链路 + Segmentation/G-H 行逐字符 | batch8 |
| 9 | 地网移除回归：GW+EX+FR 简单样例 → G/H 第 3 字段固定 0 | convert G/H 行 | verify_radials_removal |
| 10 | W8BYA 原文件全流程（报障修复 1 的验收样例） | 真实文件端到端 | bug_source |
| 11 | 边角：中文 CM 剥离/`w1c+1` 大写容错/空输入/无 FR 空频率告警 | hasChinese/removeChinese/freq 校验 | v02 手工走查项 |
| 12 | **双跑 diff 基准集**：#1–#10 全部输入在 v02/v03 各跑一遍 | 三元组（输出文本/alerts/warns）逐字符比对 | M6 验收器 |

## 2–9. 用例明细与预期值

**全部预期值与 v02 文档一致**，此处不重复罗列数值，指向 `..\..\NEC2MAA_converter_v02_测试用例.md`：
- §2 G/H 行预期：`0, 0.0, 0, 50.0, 120, 60, 0.0`（修复 4 后第 3 字段固定 0）
- §3 GN 映射四卡型与 S-N 降级告警文案（"Sommerfeld-Norton…请人工核查"）
- §4 设计符终版规则表（锚点/\|off\|=1 保留/\|off\|>1 收敛+告警含原段号）
- §5 GS 单位 9 形态与缩放预期（ft→±1.5240 等）
- §6 GM 用例与连续 GM 的 z 序列（-0.952|0.952|0.952,2.856|…4.76）
- §7 LD 七类型行输出（Q=1.7593、48.8178, -7.5969、50.0000, 0.0000、电导率告警含铜铝对照）
- §8 EX 类型策略表（0/5/6 映射、1/2/3/4 丢弃+告警、EX 6 复数→0.9998∠45）
- §9 校验告警（BAD LOAD ×3、几何卡去重、扫频步进=5）
- §10 参考对照 11 断言（Seg/G-H 行与 4x_DJ9BV 逐字符一致）

## 10. UI 走查清单（浏览器，M1/M5/M6 逐批过）

1. 双击 index.html：无 404/CORS/控制台错误
2. 布局核对（对 v02）：频率框整排；阻抗 R (Ω)/阻抗 X (Ω) **同排且在频率下方**；"阻抗 X"文案（非"电抗"）；地面电导率 help-text；**无地网框**
3. 五个按钮：按文件名/按原文件备注（标题模式）、提取、转换、下载
4. 文件选择/拖放读取（processInputText 路径）
5. 拖入 W8BYA 原文件：告警弹窗**顺序**与 v02 一致（GN 降级 → LD 5 → EX 说明）
6. 下载的 .maa 用记事本与 v02 输出 diff

## 11. 批次验证点对照（AGENTS.md §3 批次表）

| 批次 | 本文档章节 | 通过标准 |
|---|---|---|
| M2 | §0 基建 + utils 用例（v02 文档 §3/§5） | Node 单测全绿 |
| M3 | §6 GM 用例 + 不变量枚举 | 全绿 |
| M4 | §7 LD + §8 EX 矩阵 + exNotes 块外专测 | 全绿 |
| M5 | §10 UI 走查 1–4 项 | 人工确认 |
| M6 | §1 输入集 #12 双跑 diff + §10 走查 5–6 | 三元组逐字符一致 |
