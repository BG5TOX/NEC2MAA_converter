# NEC2MAA_converter 开发历史日志（v04 续录归档）

> v04 于 2026-09-03 从 v03 复制建立；本文件含 v03 时期完整历史 + v04 的 R21/SF1/SF2 系列续录。
> v0.4 发布归档：`docs/发布归档_v0.4.md`；backups 目录说明与 R21 语义收敛轨迹：`backups/README.md`；已完成的方案/评估类文档：`docs/archive/`（含 README）。
> 2026-09-03 收尾（终态）：R21a–R21j + SF1/SF2 全批次完成；审计 10 项闭环；AGENTS.md/发布归档/审计清单/格式文档双件套全部更新至终态；已完成过程文档 5 份归档 docs/archive；测试 16 套件 336 断言全绿。
> v03 及更早时期的归档说明：本文件由 AGENTS.md §6 历史性说明迁移而来（2026-09-02 收尾瘦身）。AGENTS.md 现仅保留现行契约与快照；本文件存全部 M/R 批次实施细节，供溯源。

## M0–M7 模块化迁移（2026-09-01）

- **M5：js/convert.js + js/app.js 完成**——convert.js：validateLoadRows（四重校验逐字）+ buildWiresSection/buildSourceSection/buildLoadSection/buildTailSections + executeConvert 编排（输出行序逐字契约）；app.js：setTitleMode/updateTitleInput/triggerManualExtract/processInputText/downloadMaa 逐字 + DOMContentLoaded 统一绑定（v02 的 onclick 五处 + 顶层 addEventListener 四段）。index.html 补三个按钮 id（btnReExtract/btnUpload/btnConvert）。六文件全流程 DOM 桩测试 25/25 PASS。
- **M5 测试教训**：extract 用参数文本而 convert 读 inputNec.value——测试桩必须同步赋值 inputNec，否则 convert 转的是残留文本；显式初始化的 elements 必须带 focus 桩。
- **M6/M7**：双跑回归与 v02 侧文档交叉引用归档于 docs/发布归档_v0.3.md。

## R 批次（MAA→NEC 方向，2026-09-01 起）

### R1 parser
双变体（A/B，俄文单星节头 `* Провода *` 兼容）+ 全分节 + 设计符解析 + 注释 ASCII 检测/聚合警告。T1 全库 722/722 解析成功。行数口径双实现对拍（Source 397/Load lc300+rjx107+S8）。λ 模式定案不存在——`99999.9`/`0.01` 在 `$$$ Taper wire set $$$` 锥形渐变区（20 文件），已被跳过逻辑忽略。

### R2 symbols + writer
`maa-symbols.js`（半径去重降序→Dn 无上限、负载逐参 L/C/Q 或 R/X、一卡一参）+ `maa-writer.js`（CM 头英文摘要+丢弃计数、GW 段 1+`0.5*Dn`+`z+h`、EX 百分比锚定、LD `2*PI*f*L1*1E-6/Q1` 表达式、GN epsr 分档、ASCII 终检断言）。T2/T3 22/22 PASS 含全库 722 转换+ASCII 0 违例。

### R1/R2 实施发现
俄文节头在 latin1/UTF-8 读取下西里尔变码——节区分类改为数据形态判别（isSourceShape）+ 未知节头 auto 兜底。浏览器 FileReader 读 CP1251 同样变码，此设计为必须项。

### R3 UI 融合（后被 R4 向导取代）
方向切换条+嗅探+executeConvertM2N+按扩展名自动切向+下载分向命名。T6 双向冒烟：W8BYA.nec→.maa→.nec′ 往返频率保真。

### R4 向导式 UI（用户决策）
开屏两 wizard-card → workScreen；MAA 方向隐藏整个 NEC 侧参数面板；CSS wizard-card 样式。

### R5 双面板改版（用户决策）
向导卡文案+格式标注；返回按钮；M2N 方向新增完整参数面板（m2nPanelWrap）：天线名称双模式/频率/阻抗/坐标转换/地面/分段/源负载可编辑框。extractM2nPanel 填面板→用户微调→转换读面板覆盖→writeMaaToNec。

### R5b 文案与地面预设（用户七项决策）
返回按钮"⬅ 返回"；M2N 地面类型加 -1 Sommerfeld-Norton（writer 既有 -1→GN 2 映射）；"天线名称"→M2N 双模式文案统一；"地面参数 (输出 GN 卡)"→"地面参数"；坐标轴转换文案；真实地面预设下拉 27 项 4NEC2 Ground.txt 同源（σ S/m→mS/m ×1000）。

### R5b 补充修复（返回后界面异常）
backToWizard 未复位 setDirection 改掉的 UI 状态（按钮文案/accept/placeholder/双面板显隐）——二次发现。**纪律：app.js 触碰 UI 状态的函数新增字段时必须同步其复位函数**。
另发现重复 `<body>` 标签已删。
**核心 bug**：backToWizard 设 `wizardScreen.style.display=''` 会移除初始内联 `display:flex` → 卡片不再居中。修复：显式 `'flex'`。**纪律升级：显隐切换必须显式设目标值，禁用空串复位；测试桩须保留初始内联 display**（r5b_backfix2 的桩基建即为此）。

### R6 εr 显式填写（后被 R11 全删）
σ 分档推导 εr+提示文字+自动/手动标志。9/9 PASS。

### R7 自由空间禁用 σ/εr（用户决策）
地面类型=自由空间(0)禁用 σ/εr 输入，syncGroundDisabled()。placeholder 改"典型值（自动）/ 手动填入"。

### R8 载入模型后地面联动修复（用户报障）
根因：ZZ1_hen_DL2KQ.maa 的 G/H 行是单星 `*G/H/M/R/AzEl/X*` 形态 → parser 未识别 → ground=null。修复：节头识别扩展单星形态；segment 节补俄文。全库 722 回归 0 失败。

### R9 六项 UI 改动（用户决策）
预设→自动识别 σ 回读；自由空间清空 σ/εr+隐藏提示；预设首项文案；"载入代码"→"载入模型"；基本参数+label 上方；currentFileName 保留扩展名+下载命名基名去扩展名。

### R10 G/H 行字段语义修正（用户权威定名，推翻 OpenNEC 文档）
G/H/M/R/AzEl/X：1=G 地面类型, 2=H 附加高度, 3=M 材料序号, 4=R 阻抗实部, 5=Az 6=El 后瓣F/B统计, 7=X 阻抗虚部。parser ground 改 {gtype,height,material,refZ,az,el,x}；writer SY h=、material>0 提示、删 radials；N2M 面板材料/高度输入。"阻抗 X"→"阻抗 jX"。

### R11 N2M 材料选项 + 全手动地面（用户决策）
N2M："地面电导率"输入移除→"天线材料"下拉（0-6）+"附加高度 (m)"输入，G/H 行输出 {g_ground},{add_height},{ant_material},…。M2N 自动推导全面移除（deriveEpsr/autoDeriveEpsr/m2nEpsrAuto/提示元素/联动/自动识别恢复）；预设首项"自定义"；σ/εr placeholder"手动填入"；真实地未填全拦截。

### R12 修正版 MAA 格式文档
`docs/MAA file format (corrected).md`（412 行纯英文）——review OpenNEC 原文档修正 7 处错误（G/H 字段语义、epsr 分档伪逻辑删除、Load 行实测形态、变体分布官方库数据），标 [CORRECTED]+Erratum summary。

### R13 简明格式参考
`docs/MAA format concise reference.md`（176 行纯英文）——速查版：双变体布局图/线行 8 字段/源设计符/负载 µH-pF 陷阱/附加节/解析器检查清单 6 条/最小示例。

### R14 分段参数（用户四项决策）
N2M"网格自动分段"→"自动分段参数"；M2N"分段参数"节：强制分段开关（默认不勾→密度禁用+段数 1+警告常显；勾选→密度启用+警告消失+CM Forced 行）；分段密度 Segs/λ（默认 25，λ=299.7925/f，GW 段数=ceil(len·ρ/λ)）；无效密度拦截。

### R15 分段参数文案细化（用户六项决策）
checkbox+文字同行左对齐"打开强制分段"；预设首项"自定义"；CM Note "NEC auto segmentation must be enabled."；提醒最末行；未勾选不显默认值；提醒文字定稿。

### R16 分段布局修正 + 首次进入禁用（用户实测反馈）
文字左 checkbox 右（flex-direction:row !important）；首次进入自由空间 σ/εr 禁用未生效→HTML 初始 disabled 属性保底；σ 初始 value ""。

### R17 提醒框位置修正（用户实测反馈）
提醒框移出 .control-group（grid item 会并排）成 section 直接子级，margin-top:12px；controls row-gap:14px。R16 曾错误包进 control-group，R17 纠正。

### R18 CM 首行重复修复（用户实测报障）
根因：executeConvertM2N 的 `cmTitleOverride = removeChinese(...)||'Converted_Antenna'` fallback 占位名被追加 + 文件名模式标题框内容与首行前缀同源必重复。修复：仅自定义/.maa 原标题模式（非 ^Converted from 前缀）才作 override；writer 空串显式判空不追加。

### R19 .maa 原标题独立首条 CM 卡（用户需求）
.maa 首行 title 单独作第一条 CM 卡置于 "Converted from" 之前。titleAscii 门禁（^[\x20-\x7E]*$）：非英文/控制字符标题阻断进 Dropped 聚合卡；Converted 行不拼标题；override 仅追加行尾不与首卡重复；ASCII 终检兜底。全库 640/640 有 ASCII 标题文件首卡=原标题。

### R20 两种真实地面默认 Average 预设（用户需求）
gtype 2/-1 时 σ=5 mS/m、εr=13，预设下拉同步选中 Average；applyRealGroundDefault() 统一三处调用（类型 change/提取路径）。自由空间/理想不变；改选其他预设/自定义即覆盖。

## 陈旧测试修复记录（R20 期间，非行为回归）
经 R18 备份比对确认 r7/r9/r11/r14 失败为陈旧断言（R11–R17 演进未同步测试），全部按现行契约更新。

## S1 安全审计批次（2026-09-03）
- 收尾审定后用户要求全面安全审计+内存泄漏排查：11 组 Node 探针实证（evalExpr 注入链、符号键正则元字符、GM NRPT 放大 OOM、ReDoS 病态输入、NaN/Infinity 传播、CM 换行注入、createObjectURL 泄漏、事件/状态驻留模式）
- 结论落盘 `docs/安全审计与内存风险清单.md`（8 项风险 + 10 项负面结论备案 + 修复优先级）；AGENTS.md §3 改为风险列表。修复未实施，待用户逐项决策
- 关键实证：evalExpr 大写化构成隐性沙箱（S1 接受）；`SY A(=5` 可崩 UI（S2 待修）；`GM 0,1e8` 3 秒 OOM（M1 待修）；override 含 \n 可伪造 NEC 卡（S3 待修）；下载 blob URL 不 revoke（M2 待修）
- **v0.4 复核版（同日晚）**：用户要求按清单重检项目代码并更新文档——S2/S3/M1/M2/R1/R2 逐条对 v04 实测（全部未修仍在，含复现）；R21 新代码面增量审计发现 **M3**（readFileSmart ASCII 路径 `fromCharCode.apply` 栈溢出，200KB 纯 ASCII 实证——R21g 引入的普通文件即触发的回归，优先级列首）、**S4**（`$$$` 计数悬空无告警）、**R1 锥度扩展**（L/R 非有限值静默吞锥度/半径变异）；负面结论新增 3 项（MUTATE 生命周期、TextDecoder 回退、探测性能）；AGENTS.md §3 同步（S4/M3 入表，优先序 M3→S2→M2→M1→S3→R1/R2/S4）

## R21 锥度振子重建（v04，2026-09-03，同日完成）
- 用户批复 C1/C2 按建议实施：单位=米（"R(mm)" 为 UI 语义）；Type 文件存 0–3=UI 编号−1，0/2=中心向两端对称、1/3=起点向末端顺序（用户确认 C2 系其笔误）
- **R21a**：maa-parser.js `$$$` 区由跳过改捕获（taperDefs.byName Map，计数行+定义行+防御告警）；新建 maa-taper.js expandTaperedWires（对称/顺序展开、尾对哨兵裁断、累计不足沿用末对+告警、零长跳过、未定义名 fallback 1mm+告警、type 越界兜底顺序型）；index.html 引入（parser 后 symbols 前）。单测 21/21
- **R21b**：maa-writer.js 接入（buildMaaSymbols 吃 rebuilt.wires → D 符号自动含子段半径；GW 遍历重建子段；anchorToSegField 锥度线返回 "tag, seg" 复合字段——边界→后继子段段号 1、内部→局部百分比、单子段线走原路径；splitSegField/formatExLine 辅助）；CM 头加 `CM Tapered wires: N→M` 行 + warnings 汇总提示（含台阶半径电气影响提醒）
- **验证**：r21_taper_rebuild.js 40/40——jp2000 金标准（10 线→66 GW 逐线弧段/半径明细、EX w5c→`EX 0, 30, 1`、D1–D4=0.015/0.0125/0.0095/0.006、CM/Material 行、ASCII）、4EL20HM（type1 顺序 3 段半元+type2 对称 6 段寄生）、dx415tt（type0 对称截断 6 段+type3 顺序 4 段）、边角矩阵、VDP40B 无锥度逐字节回归、**723 文件 hash 红线（702 不变/20 库锥度文件+jp2000 变化）**；全库 14 套件 289 断言零退化
- **方案手算表勘误**（实施中发现）：方案 §1.5 把 -0.003 组 L2=0.52 误写为 0.9（取自 -0.002 组）→"截断 4 段"错误，实为尾 0.24m 共 6 段、jp2000 全线无截断共 66 段（非 58）；dx415tt 寄生组首对 L=2 非 1 → 截断 6 段（非 8）。代码输出与修正后手算完全吻合，正确值固化于 r21 断言
- **遗留 T5 新增待测项**：锥度锚点子段内部百分比（43.2% 形态）、子段边界整数段号（"30, 1" 复合段字段在 4NEC2 中的解析——GW tag 30 的段 1 而非原线 5）、相连台阶半径 GW 的 4NEC2 几何/电流连续性实测

## SF2 质量修复批次（2026-09-03，用户批复 S3/R1/R2/S4——审计全部可修复项闭环）
- **S3**：writer 头部 `oneLine()` 单行净化（`[\x00-\x1F]+`→空格）统一应用于 maaTitle/cmOverride/fileName——override 含 `\nGW 1 999…` 折叠为同卡空格文本，伪造卡注入面消除
- **R1**：writer 有限值门禁——频率非有限正数/导线坐标半径（含锥度子段）非有限 → `[错误]` 告警+抛异常拦截不产出；半径零/下溢 → `[提示]` 告警产出。实测 z=NaN/1E999 频率拦截、1E-999 半径（JS 下溢为 0）提示；发现两处更优前置防线：锥度 def R=NaN 在 parser 对级校验即拒收、R1 下溢检查须含 rad=0
- **R2**：.maa 原标题卡 70 字符截断（69+`~`，同注释策略；恰 70 不截；库内无超长 ASCII 标题故 723 hash 基线 0 漂移）
- **S4**：`$$$` 计数悬空三处告警（节头切换/注释节切换/EOF）——"声明 N 个组合, 实际仅解析到 M 个"；EOF 悬空(5/0)、节头中断(3/1 定义保留)、jp2000 全量(10/10) 三态验证
- 验证：新增 `tests/sf2_quality_fixes.js` 23 断言；全库 16 套件 336 断言零退化（723 hash 基线 0 漂移）。已备份 preSF2/SF2 双快照（writer/parser）
- **审计闭环**：S1 清单 10 项——SF1 修 4（M3/S2/M2/M1）+ SF2 修 4（S3/R1/R2/S4）+ S1 接受 + I1 备查；AGENTS.md §3 与清单同步

## SF1 安全修复批次（2026-09-03，用户批复 M3/S2/M2/M1）
- **M3**：readFileSmart 纯 ASCII 快速路径 `String.fromCharCode.apply(null,buf)` → `new TextDecoder('utf-8').decode(buf)`（原 200KB 即栈溢出——R21g 引入的回归；ASCII 是 UTF-8 子集恒无损）
- **S2**：evalExpr 符号键构造正则前字面量化 `key.toUpperCase().replace(/[.*+?^${}()|[\]\\]/g,'\\$&')`（原 `SY A(=` 抛 Unterminated group 直穿 UI；元字符键静默不替换为安全语义）
- **M2**：downloadMaa 下载后立即 `URL.revokeObjectURL(url)`（引用已被浏览器捕获）
- **M1**：collectWires GM 复制加导线总量预算 200,000——`wires.length×(nrpt+1)` 超限即停止扩展+告警（原 `GM 0,1e8` 3 秒 OOM → 现 1ms 拦截；预算内 5×40000 与小复制/原地旋转路径不变）
- 验证：新增 `tests/sf1_security_fixes.js` 19 断言（含 1e8 拦截实测、1MB ASCII 直解、GM 绕 Y 30° 原地旋转回归、全链语法+W8BYA N2M 冒烟——W8BYA 测试文件已移至 `AntennaFiles-OLD-master_天线模型收藏/…/HFcollinear/`，sf1/m5 路径同步）；全库 15 套件 307 断言零退化。已备份 preSfix/Sfix 双快照（utils/geometry/app）
- 审计清单/AGENTS.md 同步：S2/M1/M2/M3 标记已修复，剩余 S3/R1/R2/S4

## R21h 弹窗提示排版优化（2026-09-03，用户报障：多条提示黏连）
- 用户反馈：M2N 转换/解析弹窗内多条提示文字紧黏。原格式 `join("\n")` 单换行分隔，长句连排
- 统一改为**编号 + 空行分隔**：`map((w,i)=>(i+1)+'. '+w).join("\n\n")`，共 8 处——app.js 2 处（MAA 解析提示 / MAA→NEC 转换提示）+ extract.js 6 处（GM 旋转 / LD 失败清单 / 并联 RLC / LD 5 电导率 / EX 激励 / 模糊点位）；convert.js 的坏 Load 行弹窗保持单 \n（数据行清单非长句）
- 全库 14 套件零退化（弹窗文案非测试断言对象；m4/m5 的 alert 内容断言不受影响——检查确认 m5 仅有 alert 触发类断言）。已备份 R21h 两文件

## R21g 版本标记清理 + CP1251 编码探测（2026-09-03，用户报障乱码）

- v0.3 标记清理：index.html 向导/工作区徽标 → v0.4，发布日期 → 2026.09.03，writer CM 头 `by NEC2MAA v0.4.`。实施波折：首次用 PowerShell -replace 直接改文件导致 UTF-8 中文全损（Get-Content/Set-Content 编码陷阱）→ 从备份恢复后改用 edit 工具
- **乱码报障**（jp2000 测试）：拖入 .maa 后输入框与解析字段显示 `*** 業嫔銆?***` 等变码。根因：`FileReader.readAsText` 默认 UTF-8 读 CP1251 俄文 .maa → 节头字节序列解码成替换字符/乱码。此前 parser 靠形态判别兜底使**转换链路正确**（w5c/锥度/地面全部正常），但显示层直接呈现乱码，且俄文节头关键词不中（segParams=null 等 AutoSeg 节丢失——只影响 CM Original 摘要不输出 dm1，不影响 NEC 几何）
- 修复（app.js 新增 readFileSmart）：readAsArrayBuffer → ①无 ≥0x80 字节（纯 ASCII）直接文本 ②UTF-8 试解含 U+FFFD（非法序列铁证）→ TextDecoder('windows-1251') 重解码 ③否则用 UTF-8 结果。文件选择与拖放两路径接入
- 修复效果（Node TextDecoder 模拟验证）：jp2000 节头恢复真西里尔 `*** Источ. ***`/`*** Нагрузка ***`/`*** Автосегм ***`/`$$$ Установки комб. пров. $$$`，parser 俄文关键词精确命中（segParams 恢复 {dm1:800,dm2:120,sc:1.6,ec:4}），转换输出 EX/GW/锥度与修复前逐字节一致（形态兜底与关键词命中殊途同归）
- **hash 基线换代**：v0.3→v0.4 CM 标记使全部 723 文件输出变化 → 旧 preR21 基线（"702 不变/21 变"断言）失效；新基线 preR21h_723_output_hashes.json（v0.4+R21f 快照），r21 断言改为"723 全一致 0 漂移" + 21 锥度文件清单固化（锥度语义再变时须重审基线）。全库 14 套件零退化。已备份 R21g 三文件
- 文件读写纪律强化：**中文/UTF-8 内容文件禁用 PowerShell 文本管道改写（Get-Content/Set-Content/-replace 编码陷阱），一律用 edit 工具**；此教训已付一次全文件损坏代价（靠 backups 恢复）

## R21f 对称锥度长度最终语义定案（2026-09-03，用户公式裁决）


- R21e 后用户再次纠正并给出**决定性公式**："B1 语义是对的。L1=中心节的整体长度，L2~L10=更细锥度节的单侧长度。比如 jp2000 第 10 根振子 = L3+L2+L1+L2+L3 = 5.1m"——即 **L1=中心节整体长（半宽 L1/2），L2..Ln=每侧全长**（R21c/R21e 把 L1 当半长、R21d 把全部 L 当总宽均不正确；R21f=L1 总宽 + 其余每侧）
- 修改仅一行：expandCenterOut 首节 `half = p.L/2`（原 `p.L`）；外侧节逻辑不变
- 全套手算验证闭合：jp2000 w10 五段 1.03+0.52+2.0+0.52+1.03=5.10 与用户公式逐字吻合（新增强断言）；w1 中心节 2m@15+每侧 1.4/1.4+尾 3.15@6；8EL6MW 恢复 3 段（中心 2m@7mm+尾 0.46@5mm——L1/2=1.0 < 半长 1.46 故尾出现）；4EL20HM 寄生中心 1.8m@30+每侧 1.8@25+尾 2.705@20（三管标题吻合）；dx415tt 寄生 7 段
- r21 46/46（新增 w10 公式断言）；全库 14 套件零退化；723 hash 红线不变。已备份 `maa-taper.js.R21f.bak`
- **四轮语义收敛终局（R21c→R21d→R21e→R21f）**：正确语义只能来自用户对原程序的确切掌握；用户给出的显式公式（如 w10=5.10）是最高效的裁决物——一次公式对拍胜过全部间接证据推理

## R21e 对称锥度长度最终语义（2026-09-03，用户二次裁决：只有 L1 是半长）【已被 R21f 推翻】
- R21d 发布后用户再看输出仍判不对："并不是所有锥度的长度都是半长，只有 L1 是半长"——即 **L1 = 中心节半长（跨中心单段 `[c−L1, c+L1]`，总宽 2·L1），L2..Ln = 各外侧节每侧长度**。恰为 R21c 时代的语义（R21d 的"全部 L/2"系过度泛化）
- 处置：从 preR21d 备份恢复 expandCenterOut（R21c 实现），更新注释/文档标注 R21e 最终语义；r21 断言全部改回 R21c 数值并重标注（jp2000 [7,5,5,5,7,5,5,5,7,5]=56、w1 中心节 4m@15、尾 0.75；dx415tt 寄生 5 段；4EL20HM 寄生中心 3.6m@30+每侧 1.8@25+尾 1.805@20——"30/25/20mm Pipe"三管全现，与标题吻合即本语义佐证）
- 8EL6MW 断言改写：L1=2.0 > 全部元件半长 1.29~1.50 → 中心节截到整根、每元件 1 段均匀 7mm、尾对(5mm)不出现——用户看过 R21d（B1）3 段锥度输出后仍裁决 R21e，接受此形态（MMANA 自身对该文件即如此渲染）
- 验证：r21 45/45；全库 14 套件零退化；723 hash 红线结构不变。已备份 `maa-taper.js.R21e.bak`（preR21e/preR21d 序列完整保留）
- 三轮语义收敛教训（R21c A→R21d B1→R21e A+L1 半长特例）：外部格式语义裁决必须以**用户在原程序中的实测**为准；工程侧把判别文件断言固化（4EL20HM 三管标题/8EL6MW 均匀段）随语义演进改写，每次改判全量回归

## R21d 对称锥度 B1 语义修正（2026-09-03，已被 R21e 推翻，存档）
- 用户报障"锥度转换似乎有问题，检查每根线段是否完全依据指定组合"→ 三层审计：①全库 77 根锥度线独立参考实现逐弧比对 0 失配（代码忠实执行其解读）；②定义名命中/计数/引用链全对；③审计中发现**语义歧点**——对称型 L 的长度语义
- 判别器 **8EL6MW**（6m 八木）：8 根元件半长 1.29~1.50 全部 < 首对 L=2.0——旧 A 解读（L=从中心起每侧长度）下所有元件退化为均匀单段、尾对(5mm)永不出现，设计意图完全落空；B1 解读（L=节跨中心总宽）下 中心 2m@7mm + 尾 0.46m@5mm = 正确 6m 锥度八木。辅证 Short 160m GP（B1 顶部收到 18mm 细端符合设计；A 停在 30mm 粗端）。jp2000/4EL20HM 两读法算术均闭合无法判别。**用户在 MMANA 中实际核对后裁决 B1 正确**
- 修复（maa-taper.js expandCenterOut）：`halfSpan = p.L / 2`（哨兵=到端点）；首节跨中心单段 `[c−L1/2, c+L1/2]`；后续节每侧 L/2 依次外推；截断/无哨兵余段逻辑同步换半宽口径
- B1 下输出变化：8EL6MW 每元件 1→3 段（恢复锥度）；jp2000 w1 中心节 4m→2m@15mm、尾 0.75→3.15m@6mm（[7,5,5,5,7,5,5,5,7,5] 仍 56 段）；4EL20HM 寄生中心节 3.6→1.8m、尾 1.805→3.605m（5 段）；dx415tt 寄生 5→7 段（断言手算初误"每侧 0.5+0.5"漏算各自半宽，修正为半侧累计 2.0+尾）
- 验证：r21 45/45（新增 8EL6MW B1 判别器断言固化：8×3 段、w1 尾 0.46@5 + 中心 2m@7 + 尾 0.46@5）；**B1 参考实现全库复审 77 根 0 失配**；全库 14 套件零退化；723 hash 红线结构不变。已备份 `maa-taper.js.R21d.bak`（preR21d 前置备份）
- 教训：方案期"算术闭合"不等于语义正确——8EL6MW 这类**边界压力文件**（半长 < 首对 L）才是语义判别器；后续对存疑格式语义，优先在全库找极端几何反例而非仅对拍自洽
- **注（R21e 后）**：本批次 B1 语义（全 L/2）已被用户二次裁决推翻——L2..Ln 实为每侧全长，仅 L1 是中心节半长。详见下条 R21e

## R21c 对称锥度跨中心合并（2026-09-03，用户需求；R21e 时其实现经 preR21d 备份恢复后成为最终形态）
- 用户指出：对称锥度（中心向两端）最内侧第一对不应把中心点断开——原实现把首对拆成左右两弧 `[c-L1,c]`+`[c,c+L1]` 同半径相接，馈电锚 `w?c` 恰落段交界（NEC 坏实践）
- 修复（maa-taper.js expandCenterOut 重构）：首对直接产出**跨中心单段** `{a: c-half, b: c+half}`，后续对外照旧左右两弧；输出空间序 = 左外侧(a 升序) + 跨中心段 + 右外侧
- 效果：每对称线子段数 −1（jp2000 [8,6,…]→[7,5,…] 66→56）；馈电锚落跨中心段内部 50%（jp2000 `EX 0, 30, 1` → `EX 0, 26, 50%`——从"边界段号 1"变为标准百分比锚，反而更规整）；4EL20HM 寄生 6→5、dx415tt 寄生 6→5；顺序型不变
- 实施波折：初版合并检查取 `left[last]` 而首对实际在 `left[0]`（合并未生效被探测发现）→ 重构为首对直建单段；左外侧排序曾误用 a 降序（弧段乱序）→ 修正 a 升序
- 验证：r21 43/43（新增 w1 跨中心段 [3.55,7.55]/GW4 `0, -2, 0, 0, 2, 0, 0.5*D1`、w3 跨中心 [0.76,4.76]、w5 源线 50% 内锚、4EL20HM w2 跨中心 [3.605,7.205]、连续性、723 hash 红线）；全库 14 套件零退化。已备份 `maa-taper.js.R21c.bak`（含 preR21c 前置备份）
- T5 待测项更新：跨中心段 50% 内锚已是 NEC 标准形态（优于 R21b 的 "tag, 1" 边界锚）；"tag, 1" 复合段字段仍存在于 b/e 锚与截断场景
