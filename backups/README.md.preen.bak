# NEC2MAA Converter

**NEC ⇄ MMANA-GAL 天线模型双向格式转换工具** — 双击即用的纯前端单页应用（无需安装、无需服务器、无需构建链），由 BG5TOX 开发。

支持 **4NEC2 (.nec/.inp)** 与 **MMANA-GAL (.maa)** 两种天线仿真格式之间的双向转换，界面支持**简体中文 / English** 运行时切换。

---

## 快速开始

1. 下载/克隆本仓库到任意位置；
2. **双击 `index.html`**，在浏览器中直接打开（file:// 协议，无需任何服务器或依赖安装）；
3. 开屏选择转换方向（4NEC2 → MMANA-GAL 或反向），拖入模型文件或点击导入；
4. 在参数面板中核对/微调参数，点击「执行转换」；
5. 下载转换结果（命名自动为 `<原名>_converted.maa/.nec`）。

> 推荐 Chrome / Edge。界面语言通过右上角按钮切换，默认简体中文，选择会被浏览器记忆。

---

## 主要功能

### 方向一：4NEC2 (.nec) → MMANA-GAL (.maa)

**输入侧支持的 NEC-2 卡片：**

| 类别 | 卡片 | 处理方式 |
|---|---|---|
| 几何 | `GW`（直导线）、`GX`（镜像）、`GM`（旋转/平移/复制） | 单遍按文件顺序处理，GM 含多轴旋转矩阵与 NRPT 复制语义 |
| 几何（告警直通） | `GA` `GH` `GR` `SP` `SM` | 不支持转换，产生明确告警并提示人工核查 |
| 激励 | `EX 0/5/6` | 映射为 MMANA 电压源；EX 6 电流源按数值近似映射并告警；EX 1–4（平面波/点源）无对应概念，丢弃+告警 |
| 负载 | `LD 1/2/3/4/5` | 串联 RLC/并联 RLC（单频等效）/串联阻抗/并联导纳（严格等效）/电导率（提示手设）；LD 0/6/7 拒收+注释留存 |
| 环境 | `GN`（-1/0/1/2 四类地面）、`FR`（扫频取 F1） | GN 2 (Sommerfeld-Norton) 降级为 MININEC 真实地并告警 |
| 符号 | `SY`（一卡一参）、`GS`（缩放，含 mm/cm/m/in/ft 单位后缀） | 表达式安全求值，支持 SIN/COS/TAN/SQRT/ABS/EXP/LOG/PI 与 uH/nF/pF 单位字 |
| 注释 | `CM`/`CE` | 保留进输出注释区（纯 ASCII 门禁） |

**输出 .maa 特性：** 双变体识别；导线半径/坐标换算米制；源/负载行采用 MMANA 手册设计符语法（`w?c/b/e±offset`）；自动分段参数（DM1/DM2/SC/EC）与 G/H 行 7 字段（地面/附加高度/材料序号/阻抗/F-B 统计范围）完整输出；不支持结构（TL 传输线等）明确告警并写入注释尾部。

### 方向二：MMANA-GAL (.maa) → 4NEC2 (.nec)

**输入侧支持的 .maa 特性：**

- **双文件变体**（A 紧凑计数行 / B 分节头），含俄文版单星节头（`* Провода *`）兼容——节区按数据形态判别，不受 CP1251 编码变码影响；
- **CP1251 编码自动探测**：俄文存盘的 .maa 文件按 UTF-8 试解出现替换字符时自动切换 windows-1251 重解码；
- **导线/源/负载**：8 字段导线行、`w?c/b/e±offset` 设计符源（相位/幅度）、两种负载形态（L-C-Q 串联 / R-jX 固定阻抗）；
- **`$$$ Taper wire set $$$` 锥度振子（本项目核心贡献）**：负半径导线按定义表**重建为多根首尾相连的不同半径 GW 子段**——完整实现对称型（中心向两端）与顺序型两种展开语义、末梢哨兵（L≥99999）、中心节跨中心合并与馈电锚 50% 定位（详见 `docs/MAA format concise reference.md` §5a，含用户公式验证的长度语义定案）；
- **Segmentation / G/H 参数、###Comment### 注释**（非 ASCII 内容按策略丢弃+聚合告警）。

**输出 .nec 特性：** CM 英文摘要头（原标题/来源/锥度重建/材料提示）；SY 符号卡一卡一参（半径降 D1..Dn、负载 L/C/Q、f/h 基础符号）；GW 半径 `0.5*Dn` 表达式与 `z+h` 垂直偏移；EX **百分比锚定**（`50%`/`1`/`100%`）与锥度子段精确定位（如 `29, 43.2%`）；LD 表达式（`2*PI*f*L1*1E-6/Q1`）；GN 四类地面映射（含 σ/εr 面板输入与 27 项 4NEC2 同源地面预设）；FR 频率；**输出强制纯 ASCII**（终检断言）。

### 通用

- 文件拖拽或按钮导入，按扩展名自动切换方向，粘贴 .maa 特征文本自动嗅探；
- 转换前参数面板全部可编辑（频率/阻抗/坐标轴对调/地面/分段密度/源/负载/标题模式）；
- 所有无法精确映射的语义均产生**编号告警**（屏显跟随界面语言，写入输出文件恒为英文）；
- 语言包架构：新增语言只需在 `js/i18n/` 复制一份语言包文件（详见下文「扩展」）。

---

## 项目历史

| 版本 | 日期 | 内容 |
|---|---|---|
| v0.1–v0.2 | 2026-09-01 前 | 单文件原型：NEC→MAA 方向、表达式求值、设计符定位 |
| v0.3 | 2026-09-01~02 | 模块化迁移（9 文件工程，M0–M7）；M2N 方向建立（R1 parser / R2 symbols+writer / R3–R5 双面板 UI / R8–R20 语义修正与契约固化，含 G/H 字段语义权威定名、地面预设同源 Ground.txt） |
| v0.4 | 2026-09-03 | **R21 锥度振子重建**（a–f 四轮语义收敛：jp2000 金标准 10→56 子段、w10 用户公式 5.10 m 定谳）；CP1251 编码探测；**安全审计闭环**（SF1/SF2 共 8 项修复：注入净化/有限值门禁/资源预算等） |
| v0.5 | 2026-09-04~05 | **中英文界面与运行时切换**（i18n-1..4：独立语言包架构 + 告警双视图——屏显跟随语言、输出文件恒英文）；723 文件 hash 基线重审定；发布后 6 轮 UI 验收修正（切换按钮/国旗徽章/行高归一/资源夹） |

开发方法论细节（批次记录、语义收敛轨迹、测试教训）见 `docs/开发历史日志.md` 与 `docs/archive/`。

**质量保障：** 17 套回归测试 / 357 断言全绿，包括官方 722 文件库全量转换冒烟、723 文件输出 hash 基线 0 漂移断言、锥度金标准（jp2000）与全库 21 个锥度文件清单固化。测试运行：`node tests/<script>.js`（部分套件依赖本地天线文件库，详见 `tests/` 内说明；`i18n_lang.js` 无外部依赖可独立运行）。

---

## 参考的外部项目与资源

| 项目 | 用途 |
|---|---|
| **[MMANA-GAL](http://www.gal-ana.de/basicmm/)**（Alexandre Schewelew 等） | .maa 格式的定义与官方手册；官方示例库（722 文件）作为解析/转换的全量回归语料 |
| **4NEC2**（Arie Voors） | .nec 格式工作基准；其 Ground.txt 地面预设表（27 项土壤参数）经同源转录用于 M2N 地面预设下拉 |
| **OpenNEC** / NEC-2 文档 | GM 旋转矩阵实现（R = Rz·Ry·Rx 次序）参考；其 .maa 格式描述文档中的多处字段语义错误由本项目实测修正（详见 `docs/MAA file format (corrected).md`） |
| MMANA-GAL 官方 ANT 文件库（722 文件）+ 社区天线收藏集（935 文件） | .maa 变体/设计符/负载形态/锥度区分布的普查基础（格式参考文档基于全量普查编写） |
| W8BYA Collinear 7 MHz、jp2000_147.maa 等社区天线模型 | 回归测试金标准样例 |

> 本项目为独立工具，与 MMANA-GAL / 4NEC2 / OpenNEC 无隶属关系；上述项目名称与商标归各自作者所有。

---

## 工程结构与扩展

```
index.html            # 单页应用入口（双击即用）
assets/               # 图标资源（4nec2.ico / MMANA.ico）
css/style.css         # 样式（语言切换按钮/卡片/面板）
js/
  state.js            # 全局命名空间 N2M 与状态
  i18n.js             # i18n 机制（L/LF 双视图渲染、语言切换）
  i18n/zh.js|en.js    # 独立语言包（纯数据，可直接编辑）
  utils.js            # 表达式求值/设计符定位等纯函数
  geometry.js         # GW/GX/GM 几何收集
  extract.js          # NEC 解析（N2M 方向）
  maa2nec/            # .maa 解析/锥度重建/符号推导/NEC 输出（M2N 方向）
  convert.js          # .maa 输出编排（N2M 方向）
  app.js              # UI 事件与流程
tests/                # 17 套回归测试
docs/                 # 格式文档（.maa 权威参考）/发布归档/历史日志/安全审计
backups/              # 批次快照与输出 hash 基线
```

**新增一种界面语言**：复制 `js/i18n/en.js` → `js/i18n/<code>.js`，修改 `meta` 并翻译词条（翻译不全时自动回退英文），在 `index.html` 加一行 `<script src="js/i18n/<code>.js">` 即完成——`node tests/i18n_lang.js` 会自动校验键集对齐。

**格式细节文档**：`.maa` 格式的完整权威参考（双变体/字段语义/锥度定义/解析器检查清单）见 `docs/MAA format concise reference.md` 及其姊妹篇 `docs/MAA file format (corrected).md`。

---

## 许可与致谢

本项目代码供社区自由使用（具体许可见 LICENSE 文件，如有）。感谢 MMANA-GAL 与 4NEC2 社区长久的格式知识积累，以及所有被用作测试语料的天线模型作者。

作者：BG5TOX · 版本 v0.5 · 2026-09
